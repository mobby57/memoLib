export { handlers as GET, handlers as POST } from '@/lib/auth/auth';

// Re-export authOptions for backward compatibility with files importing from this path
export { authOptions } from '@/lib/auth/auth';
