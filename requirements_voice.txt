# Dépendances pour la transcription vocale en temps réel

# Core dependencies
flask-socketio==5.3.5
python-socketio==5.10.0

# Audio recording
pyaudio==0.2.14

# Speech recognition
SpeechRecognition==3.10.1

# Installation instructions:
# pip install flask-socketio python-socketio pyaudio SpeechRecognition

# Note pour PyAudio sous Windows:
# Si l'installation échoue, télécharger le wheel depuis:
# https://www.lfd.uci.edu/~gohlke/pythonlibs/#pyaudio
# puis: pip install pyaudio‑0.2.14‑cp311‑cp311‑win_amd64.whl
