# 🎨 Architecture Frontend Expert

## 📊 Schéma Global

```
┌─────────────────────────────────────────────────────────────┐
│                      FRONTEND LAYER                         │
│                    React 18 + TypeScript                    │
└─────────────────────────────────────────────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   ROUTING    │    │    STATE     │    │   SERVICES   │
│ React Router │    │   Zustand    │    │  API Client  │
└──────────────┘    └──────────────┘    └──────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
        ┌─────────────────────┼─────────────────────┐
        │                     │                     │
        ▼                     ▼                     ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│    PAGES     │    │  COMPONENTS  │    │    HOOKS     │
│  Dashboard   │    │   EmailCard  │    │  useAuth     │
│  SmartForm   │    │   Sidebar    │    │  useEmail    │
│  History     │    │   Header     │    │  useAI       │
└──────────────┘    └──────────────┘    └──────────────┘
        │                     │                     │
        └─────────────────────┼─────────────────────┘
                              │
                              ▼
                    ┌──────────────┐
                    │   BACKEND    │
                    │  Flask API   │
                    └──────────────┘
```

## 🏗️ Structure Détaillée

```
frontend/
├── src/
│   ├── app/
│   │   ├── App.tsx                 # App principale
│   │   ├── Router.tsx              # Configuration routes
│   │   └── Layout.tsx              # Layout global
│   │
│   ├── pages/
│   │   ├── Dashboard/
│   │   │   ├── index.tsx           # Dashboard principal
│   │   │   ├── Stats.tsx           # Statistiques
│   │   │   └── QuickActions.tsx    # Actions rapides
│   │   │
│   │   ├── SmartEmail/
│   │   │   ├── index.tsx           # Formulaire smart
│   │   │   ├── FormFields.tsx      # Champs formulaire
│   │   │   └── Preview.tsx         # Aperçu email
│   │   │
│   │   ├── History/
│   │   │   ├── index.tsx           # Historique
│   │   │   ├── EmailList.tsx       # Liste emails
│   │   │   └── Filters.tsx         # Filtres
│   │   │
│   │   └── Settings/
│   │       ├── index.tsx           # Paramètres
│   │       ├── Credentials.tsx     # Credentials
│   │       └── Profile.tsx         # Profil
│   │
│   ├── components/
│   │   ├── ui/
│   │   │   ├── Button.tsx          # Bouton réutilisable
│   │   │   ├── Input.tsx           # Input réutilisable
│   │   │   ├── Card.tsx            # Card réutilisable
│   │   │   ├── Modal.tsx           # Modal réutilisable
│   │   │   └── Toast.tsx           # Notifications
│   │   │
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx         # Sidebar navigation
│   │   │   ├── Header.tsx          # Header
│   │   │   └── Footer.tsx          # Footer
│   │   │
│   │   └── features/
│   │       ├── EmailCard.tsx       # Card email
│   │       ├── AIStatus.tsx        # Status IA
│   │       └── ProgressBar.tsx     # Barre progression
│   │
│   ├── hooks/
│   │   ├── useAuth.ts              # Hook authentification
│   │   ├── useEmail.ts             # Hook emails
│   │   ├── useAI.ts                # Hook IA
│   │   ├── useWebSocket.ts         # Hook WebSocket
│   │   └── useToast.ts             # Hook notifications
│   │
│   ├── services/
│   │   ├── api.ts                  # Client API
│   │   ├── auth.ts                 # Service auth
│   │   ├── email.ts                # Service email
│   │   ├── ai.ts                   # Service IA
│   │   └── websocket.ts            # Service WebSocket
│   │
│   ├── store/
│   │   ├── index.ts                # Store Zustand
│   │   ├── authSlice.ts            # State auth
│   │   ├── emailSlice.ts           # State emails
│   │   └── uiSlice.ts              # State UI
│   │
│   ├── types/
│   │   ├── email.ts                # Types email
│   │   ├── user.ts                 # Types user
│   │   └── api.ts                  # Types API
│   │
│   ├── utils/
│   │   ├── format.ts               # Formatage
│   │   ├── validation.ts           # Validation
│   │   └── constants.ts            # Constantes
│   │
│   └── styles/
│       ├── globals.css             # Styles globaux
│       └── theme.ts                # Thème
│
├── public/
│   ├── index.html
│   └── assets/
│
├── package.json
├── tsconfig.json
├── vite.config.ts
└── tailwind.config.js
```

## 🎯 Pages Principales

### 1. Dashboard
```
┌─────────────────────────────────────────┐
│  Header (Logo, User, Notifications)    │
├─────────────────────────────────────────┤
│ Sidebar │  Main Content                 │
│         │  ┌─────────────────────────┐  │
│ 📊 Stats│  │   Quick Stats           │  │
│ ✉️ Smart│  │   📧 50  🚀 30  ⏰ 10  │  │
│ 📜 Hist │  └─────────────────────────┘  │
│ ⚙️ Sett │  ┌─────────────────────────┐  │
│         │  │   Quick Actions         │  │
│         │  │   [Smart Email]         │  │
│         │  │   [View History]        │  │
│         │  └─────────────────────────┘  │
│         │  ┌─────────────────────────┐  │
│         │  │   Recent Emails         │  │
│         │  │   • Email 1             │  │
│         │  │   • Email 2             │  │
│         │  └─────────────────────────┘  │
└─────────────────────────────────────────┘
```

### 2. Smart Email Form
```
┌─────────────────────────────────────────┐
│  ✨ Smart Email - IA s'occupe de tout  │
├─────────────────────────────────────────┤
│  📧 Destinataire                        │
│  [_____________________________]        │
│                                         │
│  📝 Type d'email                        │
│  [Demande ▼]                           │
│                                         │
│  🎨 Ton                                 │
│  [Professionnel ▼]                     │
│                                         │
│  📄 Contexte                            │
│  [___________________________]          │
│  [___________________________]          │
│  [___________________________]          │
│                                         │
│  ☑️ Envoyer automatiquement            │
│                                         │
│  [✨ Générer et Envoyer]               │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 📧 Aperçu (si généré)           │   │
│  │ Objet: ...                      │   │
│  │ Corps: ...                      │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### 3. History
```
┌─────────────────────────────────────────┐
│  📜 Historique des Emails               │
├─────────────────────────────────────────┤
│  [🔍 Recherche] [Filtres ▼] [Export]   │
├─────────────────────────────────────────┤
│  ┌─────────────────────────────────┐   │
│  │ ✅ Email 1                      │   │
│  │ À: dest@email.com               │   │
│  │ Objet: Demande...               │   │
│  │ 📅 10/12/2024 14:30            │   │
│  └─────────────────────────────────┘   │
│  ┌─────────────────────────────────┐   │
│  │ ⏳ Email 2 (En cours)           │   │
│  │ À: autre@email.com              │   │
│  │ Objet: Réclamation...           │   │
│  │ 📅 10/12/2024 14:25            │   │
│  └─────────────────────────────────┘   │
│                                         │
│  [← Précédent] Page 1/5 [Suivant →]   │
└─────────────────────────────────────────┘
```

## 🔄 Flux de Données

```
User Action
    ↓
Component Event
    ↓
Hook (useEmail, useAI)
    ↓
Service (api.ts)
    ↓
API Request → Backend
    ↓
Response
    ↓
Store Update (Zustand)
    ↓
Component Re-render
    ↓
UI Update
```

## 🎨 Design System

### Colors
```typescript
const colors = {
  primary: '#667eea',
  secondary: '#764ba2',
  success: '#10b981',
  error: '#ef4444',
  warning: '#f59e0b',
  info: '#3b82f6',
  dark: '#1f2937',
  light: '#f9fafb'
}
```

### Components
- Button: Primary, Secondary, Outline, Ghost
- Input: Text, Email, Password, Textarea
- Card: Default, Hover, Active
- Modal: Small, Medium, Large
- Toast: Success, Error, Warning, Info

## 🚀 Technologies

- **Framework**: React 18
- **Language**: TypeScript
- **Build**: Vite
- **Routing**: React Router v6
- **State**: Zustand
- **Styling**: Tailwind CSS
- **HTTP**: Axios
- **WebSocket**: Socket.IO Client
- **Forms**: React Hook Form
- **Validation**: Zod

## 📱 Responsive

```
Mobile (< 768px)
  - Stack layout
  - Hamburger menu
  - Full width cards

Tablet (768px - 1024px)
  - 2 column grid
  - Collapsible sidebar
  - Medium cards

Desktop (> 1024px)
  - 3 column grid
  - Fixed sidebar
  - Large cards
```

## ⚡ Performance

- Code splitting par route
- Lazy loading composants
- Memoization (React.memo)
- Virtual scrolling (listes longues)
- Image optimization
- Service Worker (PWA)

## 🔒 Sécurité

- JWT tokens (localStorage)
- CSRF protection
- XSS prevention
- Input sanitization
- Secure headers

---

**Status**: 🎨 Design prêt à implémenter
