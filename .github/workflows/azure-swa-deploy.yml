name: Azure Static Web Apps CI/CD (disabled)

# Conservé uniquement pour référence. Déclenchement manuel si nécessaire.
on:
  workflow_dispatch:

jobs:
  build_and_deploy_job:
    if: false # Désactivé pour éviter les déploiements en double. Utiliser azure-static-web-apps.yml.
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow désactivé. Utiliser azure-static-web-apps.yml pour SWA."
