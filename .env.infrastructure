# ============================================================================
# INFRASTRUCTURE CONFIGURATION — Memo Lib (28 janvier 2026)
# ============================================================================

# SECURITY - Required for production
NEXTAUTH_SECRET=$(openssl rand -base64 32)
NEXTAUTH_URL=http://localhost:3000

# DATABASE - PostgreSQL
POSTGRES_USER=memolib
POSTGRES_PASSWORD=memolib_dev_password_change_in_prod
POSTGRES_DB=memolib
DATABASE_URL=postgresql://memolib:memolib_dev_password_change_in_prod@postgres:5432/memolib

# AI SERVICES
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=llama2

# ENVIRONMENT
NODE_ENV=production
NEXT_TELEMETRY_DISABLED=1

# DOCKER COMPOSE
COMPOSE_PROJECT_NAME=memolib
COMPOSE_HTTP_TIMEOUT=120
