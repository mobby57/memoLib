declare module '@prisma/client' {
  // Minimal stub to satisfy TS during build without generated Prisma client
  // Methods/properties are typed as any for flexibility in demo builds.
  export class PrismaClient {
    [key: string]: any
  }
}
