apiVersion: v1
kind: Service
metadata:
  name: app-staging
  namespace: staging
spec:
  selector:
    app: app-staging
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
