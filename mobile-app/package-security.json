{
  "name": "security-fixes",
  "version": "1.0.0",
  "scripts": {
    "audit-fix": "npm audit fix --force",
    "security-check": "npm audit --audit-level=moderate",
    "update-vulnerable": "npm update @react-native-voice/voice expo expo-notifications"
  },
  "overrides": {
    "xmldom": ">=0.6.0",
    "xml2js": ">=0.5.0", 
    "semver": ">=7.5.2",
    "send": ">=0.19.0",
    "ip": ">=2.0.0"
  }
}