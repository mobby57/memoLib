# NextAuth configuration (development defaults)
# Replace values and copy to .env.local

# Public/base URL of your app (dev)
NEXTAUTH_URL=http://localhost:3000

# Secret for signing/encryption (generate once and keep secret)
# Example to generate on Linux/macOS:
#   openssl rand -base64 32
NEXTAUTH_SECRET=change_me
# NextAuth configuration
# In development, set to your local URL
NEXTAUTH_URL=http://localhost:3000
# Generate a strong random string for production
NEXTAUTH_SECRET=change-me-in-prod
