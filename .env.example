# IAPosteManager Production Environment Variables

# Flask Configuration
FLASK_ENV=production
SECRET_KEY=your-super-secret-key-change-this-in-production

# Database Configuration (PostgreSQL for production)
# DATABASE_URL=postgresql://username:password@hostname:5432/database_name
# Leave empty for SQLite in development

# SMTP Email Configuration (REQUIRED for sending emails)
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
FROM_EMAIL=your-email@gmail.com

# AI Configuration - Choose your provider
# AI_PROVIDER options: "openai" (payant) ou "llama" (GRATUIT local)
AI_PROVIDER=llama

# OpenAI Configuration (si AI_PROVIDER=openai)
# OPENAI_API_KEY=sk-your-openai-api-key-here
# OPENAI_WEBHOOK_SECRET=your-webhook-secret-from-openai

# Llama Local Configuration (si AI_PROVIDER=llama) 
# OLLAMA_BASE_URL=http://localhost:11434
# LLAMA_MODEL=llama3.1:8b
# LLAMA_TIMEOUT=30

# Server Configuration
HOST=0.0.0.0
PORT=5000

# Security
SESSION_COOKIE_SECURE=true
SESSION_COOKIE_HTTPONLY=true

# Logging
LOG_LEVEL=INFO
LOG_FILE=logs/app.log

# Rate Limiting
RATE_LIMIT_ENABLED=true
RATE_LIMIT_PER_MINUTE=60

# Monitoring
HEALTH_CHECK_ENABLED=true
METRICS_ENABLED=true