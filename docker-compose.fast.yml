version: '3.8'

services:
  securevault:
    build:
      context: .
      dockerfile: Dockerfile.fast
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - FLASK_ENV=production
      - FLASK_DEBUG=0
    restart: unless-stopped